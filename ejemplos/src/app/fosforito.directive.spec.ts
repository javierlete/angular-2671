import { FosforitoDirective } from './fosforito.directive';

describe('FosforitoDirective', () => {
  it('should create an instance', () => {
    const directive = new FosforitoDirective();
    expect(directive).toBeTruthy();
  });
});
